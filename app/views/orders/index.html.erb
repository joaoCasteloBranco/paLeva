<div class="container mx-auto px-5 mt-10">
  <h1 class="text-2xl font-bold text-gray-800 mb-6">Pedidos:</h1>

  <% @orders.each do |order| %>
    <%= link_to restaurant_order_path(@restaurant, order), class: "block bg-white shadow-md rounded-md p-6 mb-4 hover:bg-gray-200 transition-colors" do %>
      <div class="grid grid-cols-2 gap-4 text-sm md:text-base">
        <div class="font-semibold text-gray-700">
          <span class="text-gray-500">Código:</span> <%= order.code %>
        </div>

        <div class="font-semibold text-gray-700">
          <span class="text-gray-500">Cliente:</span> <%= order.customer_name %>
        </div>

        <div class="font-semibold text-gray-700">
          <span class="text-gray-500">Telefone:</span> <%= order.contact_phone %>
        </div>

        <div class="font-semibold text-gray-700">
          <span class="text-gray-500">Email:</span> <%= order.contact_email %>
        </div>

        <div class="font-semibold text-gray-700">
          <span class="text-gray-500">CPF:</span> <%= order.cpf %>
        </div>

        <div class="font-semibold text-gray-700">
          <span class="text-gray-500">Status:</span> <%= order.status %>
        </div>
      </div>
    <% end %>
  <% end %>
</div>
