<h1>Adicionar Marcadores</h1>

<%= form_with(model: [@restaurant, @marker]) do |f| %>

<div>
  <%= f.label :name, "Nome do Marcador" %>
  <%= f.text_field :name %>
</div>
  
  <%= f.submit "Adicionar" %>

<% end %>